defmodule Randomiser do
  def rand do
     list = [ 1, 2, 3]
     IO.puts "Answer is #{Enum.random(list)} 
## OR ( if you don't want to make a santence and just print the answer) 
     Enum.random(list) |> IO.puts
  end
end 
